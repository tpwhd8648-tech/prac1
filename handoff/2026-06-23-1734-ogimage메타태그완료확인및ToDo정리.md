# og:image 메타태그 완료 확인 및 To-Do 정리 인수인계 문서

작성일: 2026-06-23

## 1. 목표

og:image:width/height/alt 추가 작업 진행 여부를 점검하고,
이미 완료된 항목을 To-Do에서 제거해 할일 목록을 최신화한다.

## 2. 현재 상태

**확인 완료 (코드 직접 검증)**

- **og:image:width/height/alt 이미 전부 반영됨**: 정적 페이지 6개
  (`index.html`, `coins.html`, `gold-price.html`, `contact.html`,
  `mypage.html`, `coin-detail.html`) 및 `scripts/generate-coin-pages.js`
  템플릿 모두 이미 반영되어 있었음. Actions 자동 커밋(`e8d2315`)에
  포함된 것으로 확인.
- **코인 이미지 전부 1200×1200**: `images/products-*.png` 40개 전부
  `identify`로 확인 — 모두 1200×1200. 리사이즈 작업 불필요.
- **To-Do 항목 2개 제거**:
  - `og:image:width/height/alt` 추가 → 이미 완료 확인, 제거
  - `<img>` width/height 추가 → 이전 세션에서 불필요 판단, 제거

**코드 변경 없음** — 이번 세션은 확인/점검 작업만 진행.

## 3. 진행 단계

이번 세션 작업은 모두 완료.

1. ~~(완료) 구글 리치 스니펫 실제 적용 여부 확인~~
2. ~~(완료) 네이버 검색 노출 여부 확인 (메인 페이지 1개 노출)~~
3. ~~(완료) 구글 서치 콘솔 등록 (소유권 자동 확인)~~
4. ~~(완료) og:image:width/height/alt 반영 여부 확인 → 이미 완료 상태~~
5. ~~(완료) To-Do 항목 정리~~

## 4. 다음에 진행할 일 (To-Do)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 — **2주 후** 아래 항목 순서대로 점검:
  1. 색인 생성 → 페이지: 색인된 페이지 수 확인 (목표: 25개 내외)
  2. "색인되지 않음" 항목 원인 파악 → 🤖 조치
  3. 검색결과 성과: 노출수·클릭수·게재순위 확인
  4. 유입 검색어 보고 title/description 최적화 여부 판단 → 🤖 반영
  5. 리치 결과 탭에서 제품 스니펫 실제 노출 여부 확인
- 🔵 👤 코인 상세페이지 "관련 코인" 섹션 추가 여부 결정 (전환율 관점)
- 📢 👤 네이버 검색 노출 안정화되면 사이트명 변경 진행
- 📢 👤 콘텐츠 마케팅 / 네이버 파워링크 (보류 중)
- 🔵 👤 `coin-detail.html` JSON-LD 정적 전환 여부 결정 (우선순위 낮음)
- 🔵 👤 Organization/LocalBusiness JSON-LD 추가 여부 — 주소 실제 정보 확인 후 진행

### 🛍️ 상품 데이터
- ⚪ 👤 서울 본점 주소·전화번호 실제 정보인지 확인
- ⚪ 👤 `contact.html` 지도 「임시 주소」를 실제 주소로 교체
- ⚪ 👤 강남 지점 실재 여부 확인 → 있으면 `footer.js`에 추가
- ⚪ 👤 소셜 미디어 계정 생기면 `footer.js`에 추가

### 🎨 UI/UX
- 🟡 👤 카테고리 배너 디자인 방향 결정 (여러 세션째 보류 중)
- 🟡 👤 뱃지 shimmer 애니메이션 제거 여부 결정 → 🤖 정적 뱃지로 교체

### ⚙️ 기능/인프라
- 해당 없음

### 📋 운영/프로세스
- 🔵 👤 `구매안내` 탭 문구(결제/배송/교환·환불 등) 실제 운영 정책 검토
  → 확정되면 🤖 `shippingDetails` / `hasMerchantReturnPolicy` JSON-LD에 반영

## 5. 참고 사항

- **og:image 메타태그 현황 (확인 완료)**:
  - `og:image`: `https://tpwhd8648-tech.github.io/images/products-maple-2026.png`
  - `og:image:width`: `1200`
  - `og:image:height`: `1200`
  - `og:image:alt`: 정적 페이지는 `MIDAS BULLION 귀금속 쇼핑몰`, 코인 상세 페이지는 `${coin.name} 금화 - MIDAS BULLION`
- **코인 이미지 크기**: 전부 1200×1200 (PNG, 40개). 추후 og:image 관련
  작업 시 리사이즈 불필요.
- **구글 서치 콘솔**: 등록 완료, 소유권 HTML 태그 방식으로 자동 확인됨.
  데이터 수집까지 며칠~2주 소요. `index.html`의 Google 확인 메타태그
  삭제하면 소유권 확인 풀리므로 삭제 금지.
- **네이버 색인**: 현재 메인 페이지 1개만 노출. 코인 상세 페이지는
  JS 동적 렌더링 문제로 수집이 늦을 수 있음. 시간이 지나도 안 잡히면
  Option A(GitHub Actions 정적 생성)가 유효.
- **GitHub PAT**: 이번 세션에서 재발급됨. 만료일 미확인 — 다음 push 시
  만료 여부 먼저 확인할 것.
- **이전 핸드오프**: `2026-06-23-1724-구글서치콘솔등록및SEO점검완료.md`
