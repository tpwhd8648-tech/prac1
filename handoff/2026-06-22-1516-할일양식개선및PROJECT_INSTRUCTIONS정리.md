# 할일 양식 개선 및 PROJECT_INSTRUCTIONS 정리 인수인계 문서

작성일: 2026-06-22

## 1. 목표

할일 목록과 인수인계 To-Do 섹션의 가독성을 높이기 위해 색깔 이모지 + 담당자(🤖/👤)
양식을 도입하고, 이를 `PROJECT_INSTRUCTIONS.md`에 영구 규칙으로 저장했다.
어떤 계정에서 시작하든 동일한 양식이 자동 적용되도록 단일 진실 소스를 정비했다.

## 2. 현재 상태

**완료 (push 후 원격 반영 확인)**

- 색깔 이모지 + 담당자 양식 도입 (커밋 `222c2b6`)
  - 🔴 긴급 / 🟡 일반 / 🔵 보류 / ⚪ 사실확인 / 📢 마케팅 5개 카테고리
  - 각 항목에 🤖(Claude 처리) / 👤(사용자 직접 액션) 담당자 표시
  - `PROJECT_INSTRUCTIONS.md` 섹션 1에 양식 정의 추가

- `PROJECT_INSTRUCTIONS.md` 추가 정비 (커밋 `af46f39`)
  - 섹션 1: "위 색깔 이모지 양식으로 요약" 명시
  - 섹션 4: handoff-doc 스킬의 체크박스 양식보다 이 문서의 색깔 이모지 양식이
    우선한다는 충돌 해소 문장 추가
  - 섹션 4: To-Do 작성 예시 코드블록 추가

- 최신 인수인계 파일 To-Do 섹션 새 양식으로 재작성 (커밋 `af46f39`)
  - `2026-06-22-1436-시트셀명정리및당일수령뱃지추가.md` To-Do 섹션 업데이트

**확인 완료**

- handoff-doc 스킬(`/mnt/skills/user/handoff-doc/SKILL.md`)은 읽기 전용이라
  직접 수정 불가 — `PROJECT_INSTRUCTIONS.md`에서 명시적으로 덮어쓰는 방식으로 처리.
  스킬 파일과의 충돌 지점 2개 모두 `PROJECT_INSTRUCTIONS.md`가 우선함:
  1. 파일명 규칙: 스킬(`YYYY-MM-DD-주제.md`) → 실제(`YYYY-MM-DD-HHMM-주제.md`)
  2. To-Do 양식: 스킬(체크박스 `- [ ]`) → 실제(색깔 이모지 + 🤖/👤)

## 3. 진행 단계

이번 세션 작업은 모두 완료. 추가로 진행할 단계 없음.

## 4. 다음에 진행할 일 (To-Do)

### 🔴 긴급 / 즉시 처리
- 👤 실제 사이트(coins.html, index.html, coin-detail.html)에서 당일수령 뱃지가
  의도한 9개 상품(메이플, 브리타니아, 캥거루, 버팔로, 이글, 필하모닉, 크루거,
  판다, 코뿔소)에만 우상단 골드 톤으로 정상 노출되는지 브라우저로 확인한다
- 👤 모바일 화면에서 뱃지 크기/위치가 카드 레이아웃과 겹치지 않는지 확인한다

### 🔵 보류
- 👤 카테고리 배너 디자인 방향 결정:
  (a) 기존 unsplash 사진 3장을 골드 톤 사진으로 교체 vs
  (b) 사진 없이 그라디언트·타이포그래피 중심 미니멀 디자인으로 교체

### ⚪ 사실 확인
- 👤 `2026 말띠 1oz`(order=14)와 `2026 네스호 1oz`(order=13) 순서가 의도된 건지
  확인 → 아니라면 시트에서 두 숫자를 바꾼다
- 👤 서울 본점 주소·전화번호가 실제 정보인지 확인한다
- 👤 contact.html 지도(`#contact-map`)의 "임시 주소"를 실제 주소로 교체한다
- 👤 "강남 지점"이 실제로 존재하면 정확한 주소/전화/영업시간으로 footer.js에 추가한다
- 👤 소셜 미디어 계정(facebook/instagram/youtube/line)이 생기면 footer.js에 추가한다
- 👤 (선택) "고객센터" 페이지/링크가 다시 필요해지면 footer.js "문의 사항" 목록에 추가한다

### 📢 마케팅 / 노출
- 👤 네이버 검색 노출 여부 확인 → 안정화되면 사이트명 변경 진행
- 👤 구글 서치 콘솔 / 콘텐츠 마케팅 / 네이버 파워링크 (보류 중)

## 5. 참고 사항

- **양식 정의 위치**: 색깔 이모지 양식은 `docs/PROJECT_INSTRUCTIONS.md` 섹션 1에
  정의되어 있다. 양식을 수정하고 싶으면 그 파일만 고치면 모든 계정에 반영된다.

- **스킬 파일 충돌**: `/mnt/skills/user/handoff-doc/SKILL.md`는 읽기 전용이라
  수정 불가. To-Do 양식과 파일명 규칙은 `PROJECT_INSTRUCTIONS.md`가 우선한다는
  명시가 되어 있으므로 별도 조치 불필요.

- **메모리에도 저장됨**: 이 계정의 Claude 메모리에도 색깔 이모지 양식 규칙이
  저장되어 있음. 다른 계정에서는 메모리가 없으므로 `PROJECT_INSTRUCTIONS.md`만
  의존한다.
