# 더미 주소·전화·사업자등록번호 주석 처리 완료 인수인계 문서

작성일: 2026-06-23

## 1. 목표

실제 사업 정보(주소·전화번호·사업자등록번호)가 확정되기 전까지
더미 데이터가 사이트에 노출되지 않도록 주석 처리한다.
실제 정보가 확정되면 주석 해제 후 값만 교체하면 즉시 반영 가능한 구조로 유지.

## 2. 현재 상태

**완료 (커밋 `3fefc44`, 직접 확인)**

주석 처리된 더미 데이터 목록:

- **`footer.js`**
  - 서울 본점 주소 블록: `서울특별시 종로구 종로 1가 1번지 / 타임스퀘어 CBD / 02-1234-5678` + 영업시간
  - 사업자등록번호: `123-45-67890` (© 저작권 문구에서 분리, 번호만 주석 처리)
- **`contact.html`**
  - 전화 카드 (`02-1234-5678`, PHONE 섹션)
  - 지도 섹션 전체 (`#contact-map`, 임시 주소 텍스트 + 구글 지도 iframe)

Python으로 HTML 주석 제거 후 재검색해 3개 키워드(`종로 1가`, `1234-5678`,
`123-45-67890`) 모두 주석 밖 노출 없음 확인 완료.

**화면에 현재 표시되는 것**: `© 2026 MIDAS BULLION 귀금속 전문점.` (번호 없이)

## 3. 진행 단계

이번 세션 작업은 모두 완료.

1. ~~(완료) `footer.js` 서울 본점 주소/전화 블록 주석 처리~~
2. ~~(완료) `contact.html` 전화 카드 주석 처리~~
3. ~~(완료) `contact.html` 지도 섹션 주석 처리~~
4. ~~(완료) `footer.js` 사업자등록번호 주석 처리~~
5. ~~(완료) 주석 밖 노출 여부 Python으로 재검증~~
6. ~~(완료) 커밋 `3fefc44` 및 push 완료~~

**나중에 실제 정보 확정 시 해야 할 작업**:
1. `footer.js` 37번 줄 부근 주석 해제 → 실제 주소·전화·영업시간으로 교체
2. `footer.js` 59번 줄 부근 주석 해제 → 실제 사업자등록번호로 교체
3. `contact.html` 전화 카드 주석 해제 → 실제 번호로 교체
4. `contact.html` 지도 섹션 주석 해제 → 실제 주소 텍스트 + 구글 지도 embed URL 교체
   (구글 지도 embed URL은 maps.google.com에서 실제 주소 검색 후 새로 생성)
5. 강남 지점 실재 확인 시 `footer.js` 강남 지점 블록도 함께 추가

## 4. 다음에 진행할 일 (To-Do)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 — **2주 후** 순서대로 점검:
  1. 색인된 페이지 수 확인 (목표: 25개 내외)
  2. "색인되지 않음" 항목 원인 파악 → 🤖 조치
  3. 검색결과 성과: 노출수·클릭수·게재순위 확인
  4. 유입 검색어 보고 title/description 최적화 판단 → 🤖 반영
  5. 리치 결과 탭에서 제품 스니펫 실제 노출 여부 확인
- 🔵 👤 코인 상세페이지 "관련 코인" 섹션 추가 여부 결정 (전환율 관점)
- 📢 👤 네이버 검색 노출 안정화되면 사이트명 변경 진행
- 📢 👤 콘텐츠 마케팅 / 네이버 파워링크 (보류 중)
- 🔵 👤 `coin-detail.html` JSON-LD 정적 전환 여부 결정 (우선순위 낮음)
- 🔵 👤 Organization/LocalBusiness JSON-LD 추가 여부 — 아래 🛍️ 주소 확정 후 진행
- 🔵 👤 `apple-touch-icon` 추가 — 180×180 PNG 이미지 준비 후 진행
- 🔵 👤 `sku` 추가 — Merchant Center 연동 계획 생기면 그때 진행

### 🛍️ 상품 데이터
- ⚪ 👤 서울 본점 실제 주소·전화번호 확정
  → 확정 시 🤖: `footer.js` 본점 블록 주석 해제 + 값 교체
- ⚪ 👤 사업자등록번호 실제 번호 확정
  → 확정 시 🤖: `footer.js` footer-bottom 주석 해제 + 값 교체
- ⚪ 👤 `contact.html` 전화 카드 실제 번호 확정
  → 확정 시 🤖: 전화 카드 주석 해제 + 값 교체
- ⚪ 👤 `contact.html` 지도 섹션 실제 주소 확정
  → 확정 시 🤖: 지도 섹션 주석 해제 + 주소 텍스트·embed URL 교체
- ⚪ 👤 강남 지점 실재 여부 확인 → 있으면 🤖 `footer.js`에 추가
- ⚪ 👤 소셜 미디어 계정 생기면 🤖 `footer.js`에 추가

### 🎨 UI/UX
- 🟡 👤 카테고리 배너 디자인 방향 결정 (여러 세션째 보류 중)
- 🟡 👤 뱃지 shimmer 애니메이션 제거 여부 결정 → 🤖 정적 뱃지로 교체

### ⚙️ 기능/인프라
- 해당 없음

### 📋 운영/프로세스
- 🔵 👤 `구매안내` 탭 문구(결제/배송/교환·환불 등) 실제 운영 정책 검토
  → 확정되면 🤖 `shippingDetails` / `hasMerchantReturnPolicy` JSON-LD에 반영

## 5. 참고 사항

- **주석 위치 빠른 참조**:
  - `footer.js` ~37번 줄: 본점 주소·전화·영업시간 블록
  - `footer.js` ~59번 줄: 사업자등록번호 (footer-bottom 안)
  - `contact.html` ~131번 줄: 전화 카드 (PHONE 섹션)
  - `contact.html` ~299번 줄: 지도 섹션 전체 (`#contact-map`)
- **© 저작권 문구 처리**: 사업자등록번호만 주석 처리하고
  `© 2026 MIDAS BULLION 귀금속 전문점.`은 그대로 노출 중.
  번호 확정 시 주석 해제해서 한 줄로 합치면 됨.
- **구글 지도 embed URL 재생성 방법**: maps.google.com에서 실제 주소 검색
  → 공유 → 지도 퍼가기 → iframe src 복사해서 contact.html에 붙여넣기.
- **GitHub PAT**: 이번 세션에서 사용. 만료일 미확인 — 다음 push 시 만료 여부 먼저 확인.
- **이전 핸드오프**: `2026-06-23-1747-itemCondition추가완료.md`
