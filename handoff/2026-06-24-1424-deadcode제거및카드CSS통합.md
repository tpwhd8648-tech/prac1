# dead code 제거 및 카드 CSS 통합 인수인계 문서

작성일: 2026-06-24

## 1. 목표

코드 점검(🎨 UI/UX · ⚙️ 기능/인프라 · 📋 운영/프로세스 영역)을 통해 발견된
dead code 및 중복 CSS를 제거하고, 분산된 카드 공통 CSS를 style.css로 통합해
유지보수성을 높인다.

## 2. 현재 상태

**이번 세션 완료 (커밋 `9ee2d7c`, push 확인)**

| # | 항목 | 처리 결과 |
|---|---|---|
| 1 | `style.css` kakao-float dead CSS | ✅ 삭제 완료 |
| 2 | `script.js` ptab dead code (2곳) | ✅ 삭제 완료 |
| 3 | 카드 공통 CSS 3곳 분산 | ✅ style.css 통합 + 불일치 해소 완료 |
| 4 | `coins.html` 탭 1개 | 🔵 보류 (상품 확장 시 처리) |
| 5 | `mypage.html` 주문 내역 빈 상태 | 🟡 다음 세션 작업 예정 |
| 6 | Firebase 보안 규칙 | 🔵 작업 안 함 (최종 결정) |

**3번 통합 상세:**
- `style.css`에 `PRODUCT CARD COMMON` 섹션 신설
- 이동된 클래스: `products-loading`, `loading-spinner`, `@keyframes spin`, `product-img-placeholder`, `card-soldout`, `soldout-overlay`, `btn-cart-wrap`, `btn-buy`, `btn-buy:hover`, `btn-soldout`, `.product-card` cursor
- `btn-soldout` 불일치 해소: index.html(`cursor:not-allowed; padding:10px 16px; border-radius:3px`)을 coins.html 기준(`width:100%; cursor:pointer; padding:10px`)으로 통일
- index.html · coins.html 인라인 `<style>`에서 해당 항목 제거

## 3. 진행 단계

이번 세션 작업은 모두 완료.

1. ~~(완료) style.css kakao-float dead CSS 삭제~~
2. ~~(완료) script.js ptab dead code 삭제 (이벤트 리스너 블록 + renderProducts 내 참조)~~
3. ~~(완료) 카드 공통 CSS style.css 통합 + index.html · coins.html 인라인 제거~~
4. ~~(완료) 커밋 `9ee2d7c` push 완료~~

**다음 세션에서 해야 할 작업 (5번 — mypage.html 주문 내역):**
- 현재 `mypage.html`은 로그인 후에도 "아직 주문 내역이 없습니다." 고정 문구만 표시
- 구매 방식이 카카오톡 문의 기반이라 실제 주문 DB 연동은 없으므로,
  빈 페이지 대신 **카카오 채널 안내 또는 구매 안내 문구**로 교체하는 방향 검토 필요
- 구체적인 문구/방향은 클로가 결정 → 🤖 작업

## 4. 다음에 진행할 일 (To-Do)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 — **2주 후** (약 2026-07-07 이후) 순서대로 점검:
  1. 색인된 페이지 수 확인 (목표: 25개 내외)
  2. "색인되지 않음" 원인 파악 → 🤖 조치
  3. 노출수·클릭수·게재순위 확인
  4. 유입 검색어로 title/description 최적화 판단 → 🤖 반영
  5. 리치 결과 탭에서 제품 스니펫 실제 노출 여부 확인
- 🔵 👤 코인 상세페이지 "관련 코인" 섹션 추가 여부 결정 (전환율 관점)
- 📢 👤 네이버 검색 노출 안정화되면 사이트명 변경 진행
- 📢 👤 콘텐츠 마케팅 / 네이버 파워링크 (보류 중)
- 🔵 👤 `coin-detail.html` JSON-LD 정적 전환 여부 결정 (우선순위 낮음)
- 🔵 👤 Organization/LocalBusiness JSON-LD 추가 여부 — 🛍️ 주소 확정 후 진행
- 🔵 👤 `apple-touch-icon` 추가 — 180×180 PNG 이미지 준비 후 진행
- 🔵 👤 `sku` 추가 — Merchant Center 연동 계획 생기면 진행

### 🛍️ 상품 데이터
- ⚪ 👤 서울 본점 실제 주소·전화번호·영업시간 확정
  → 확정 시 🤖: `footer.js` ~37번 줄 주석 해제 + 값 교체
- ⚪ 👤 강남 지점 실재 여부 확인
  → 있으면 🤖: `footer.js` ~44번 줄 주석 해제 + 실제 정보 입력
- ⚪ 👤 소셜 미디어 계정(facebook/instagram/youtube/line) 생기면
  → 🤖: `footer.js` ~51번 줄 주석 해제 + 각 계정 URL 입력
- ⚪ 👤 사업자등록번호 확정
  → 확정 시 🤖: `footer.js` ~59번 줄 주석 해제 + 값 교체
- ⚪ 👤 전화번호 확정
  → 확정 시 🤖: `contact.html` ~131번 줄 전화 카드 주석 해제 + 값 교체
- ⚪ 👤 실제 주소 확정
  → 확정 시 🤖: `contact.html` ~299번 줄 지도 섹션 주석 해제 + 주소 텍스트·embed URL 교체
  → **함께 처리**: `footer.js`의 "오시는 길" 링크를 `href="contact.html#contact-map"`으로 복원

### 🎨 UI/UX
- 🟡 🤖 `mypage.html` 주문 내역 빈 상태 개선
  → 카카오 채널 구매 문의 안내 또는 구매 가이드 문구로 교체
  → 클로가 방향 결정 후 🤖 작업
- 🟡 👤 카테고리 배너 디자인 방향 결정 (여러 세션째 보류 중)
- 🟡 👤 뱃지 shimmer 애니메이션 제거 여부 결정 → 🤖 정적 뱃지로 교체

### ⚙️ 기능/인프라
- 🔵 👤 `coins.html` 탭 — 은화/컬렉터블 등 상품 카테고리 확장 시 탭 추가 필요
  (현재 금화 1개만 있음, 지금은 문제 없음)

### 📋 운영/프로세스
- 🔵 👤 `구매안내` 탭 문구(결제/배송/교환·환불 등) 실제 운영 정책 검토
  → 확정되면 🤖 `shippingDetails` / `hasMerchantReturnPolicy` JSON-LD에 반영
- 🔵 Firebase 보안 규칙 — **최종 작업 안 함으로 결정**
  (Firebase 클라이언트 API 키 공개는 설계상 허용, Firebase 콘솔에서 도메인 제한/보안 규칙으로 보호할 것)

## 5. 참고 사항

- **이번 세션에서 최종 결정된 항목**:
  - Firebase 보안 규칙(#6): 작업 안 함으로 확정. 다음 세션에서 다시 꺼낼 필요 없음.
- **`btn-soldout` 통일 기준**: coins.html 기준(`width:100%; cursor:pointer`)으로 통일.
  `createProductCard()`가 품절 상품도 클릭 시 상세페이지로 이동시키므로 `cursor:pointer`가 맞음.
- **`mypage.html` 작업 방향**: 실제 주문 DB 연동은 카카오톡 문의 기반 운영상 불필요.
  빈 상태 대신 "카카오로 구매 문의 후 내역을 확인하세요" 류의 안내 문구 + 카카오 링크 버튼으로 교체하는 방향이 자연스러움.
- **GitHub PAT**: 이번 세션에서 사용 (push 1회). 만료일 미확인 — 다음 push 시 만료 여부 먼저 확인.
- **이전 핸드오프**: `2026-06-23-1851-가격캐시롤백방지및슬라이더안전화.md`
