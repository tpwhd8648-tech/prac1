# 검색 기능 연결(데스크탑) 및 footer 신뢰도 정리 인수인계 문서

작성일: 2026-06-21

## 1. 목표

직전 인수인계 문서(`2026-06-20-0629-주요지표및푸터정리.md`)의 1순위 To-Do 5개
("footer.js 신뢰도 정리" 4개 + "헤더 검색 기능 결정" 1개)를 처리했고, 이후
사용자 요청으로 모바일 검색바는 다시 제거해 데스크탑 검색만 남긴 상태다.

## 2. 현재 상태

**완료(확인 완료 — push 후 `git ls-remote`로 원격 반영 검증함)**

- `nav.js` / `style.css` — 검색 기능은 **데스크탑에만** 존재 (커밋
  `965b202`로 추가 → `f684236`으로 모바일 부분만 다시 제거)
  - 데스크탑 헤더 검색창(`search-input` + `search-btn`)에 클릭/Enter 이벤트
    바인딩 있음. 공용 함수 `goSearch(keyword)`가 `coins.html?q=인코딩값`으로
    이동시킴(값이 비어있으면 `coins.html`로 이동).
  - `coins.html`에는 이미 `?q=` 파라미터를 받아 이름 기준으로 필터링하는
    로직이 있음(이전 세션에서 구현된 것으로 추정).
  - **모바일 메뉴(`nav-mobile`)에는 검색바가 없음.** 한 차례 추가했다가
    (커밋 `965b202`) 사용자가 모바일만 이전 상태로 되돌리길 원해서 마크업·
    이벤트·CSS(`.mobile-search` 등) 모두 삭제함(커밋 `f684236`). 모바일
    메뉴는 다시 메뉴 링크 4개(금화 보기/금 시세/구매 문의/로그인)만 있는
    원래 모습.
  - `node --check nav.js` 문법 정상 확인.
- `footer.js` (커밋 `0e93a6d`)
  - "강남 지점" 위치 블록 삭제. 주소/전화번호가 실제 정보인지 확정되지
    않았다는 사용자 확인에 따라, 화면에는 노출하지 않고 코드 내 HTML
    주석으로 "사실 확인 후 다시 추가"라고 남겨둠. 서울 본점 정보는 그대로 둠.
  - 플레이스홀더 항목(카탈로그 "금화1~4", 기타서비스 "예정1, 예정2") 삭제 +
    주석으로 사유 기록.
  - 연결할 실제 페이지가 없는 죽은 링크(회사 소개/온라인 예약/거래 안내/
    귀금속 투명성/정품 인증/서비스 약관/자주 묻는 질문/주문 내역) 삭제 +
    주석으로 사유 기록.
  - 연결 가능한 링크는 실제 페이지로 교체:
    - 카탈로그 "금화" → `coins.html`
    - "내 계정" → `mypage.html`
    - "고객센터" → `contact.html`
    - "오시는 길" → 같은 페이지 내 위치 정보 섹션(`#footer-locations`,
      이번에 id 신규 부여)으로 앵커 연결
  - 작동하지 않는 소셜 아이콘(facebook/instagram/youtube/line) 삭제 +
    주석으로 사유 기록. 카카오 아이콘은 실제 링크가 있어 그대로 유지.
  - `node --check footer.js` 문법 정상 확인.

**의사결정(코드 변경 없음, 기록용)**

- 모바일 검색바: 한 차례 구현했다가 사용자가 "일단 모바일만 예전으로
  돌리자"고 결정해 제거함. 데스크탑 검색은 유지. 모바일 검색창이 있던
  자리에 다른 콘텐츠(공지/CTA 버튼 등)를 넣는 방법도 안내했으나, 무엇을
  넣을지는 아직 결정 안 됨 — 다음 세션에서 필요시 논의.

**보안 메모**

- 이번 세션에서 GitHub PAT가 채팅에 평문으로 4회 입력됨(push 4건). 매번
  revoke 권고함. 다음 세션 시작 시 revoke 여부 확인할 것.

## 3. 진행 단계

이번 세션 작업은 모두 완료되어 추가로 진행할 단계는 없음. 다음 세션
우선순위 제안:

1. (선택) 모바일 헤더 검색 자리에 무엇을 넣을지 결정 — 그대로 빈 상태 유지
   할지, 공지문구/CTA 버튼 등을 넣을지.
2. (필수) 2순위 — mypage.html "찜한 상품" 기능 신규 구현 vs 섹션 제거 결정.
3. (필수) 2순위 — 장바구니 사이드바 신규 구현 vs 단일 구매문의 버튼으로
   통일 후 제거 결정.
4. (선택) 3순위 — coin-detail.html 관련 상품 추천, coins.html 뱃지 적용.
5. (선택) 4순위 — 카테고리 배너 디자인 교체, `.search-category select`
   활용 여부.
6. (이어지는 항목) contact.html 임시 주소 교체, 네이버 노출 확인.

## 4. 다음에 진행할 일 (To-Do)

**모바일 검색 관련**
- [ ] 모바일 헤더 검색 자리(현재 빈 상태)에 다른 콘텐츠를 넣을지 결정한다
      (예: 공지문구, CTA 버튼). 필요 없다고 판단되면 별도 작업 없이 그대로
      둔다.

**1순위에서 보류 중인 사실 확인 필요 항목 (재오픈 가능성 있음)**
- [ ] 서울 본점 주소·전화번호가 실제 정보인지 확인한다 (확인되면 footer.js
      위치 정보 그대로 유지, 아니면 본점 정보도 교체 필요)
- [ ] "강남 지점"이 실제로 존재하면 정확한 주소/전화/영업시간으로 footer.js
      주석 위치에 다시 추가한다
- [ ] 소셜 미디어 계정(facebook/instagram/youtube/line)이 실제로 생기면
      footer.js 주석 위치에 해당 아이콘을 다시 추가한다

**2순위 — 비어있는 UI 정리**
- [ ] mypage.html "찜한 상품" 기능: 찜 버튼 신규 구현 vs 섹션 제거 결정한다
- [ ] 장바구니 사이드바: 담기 기능 신규 구현 vs coin-detail.html 방식(단일
      구매 문의 버튼)으로 통일 후 사이드바 제거 결정한다
- [ ] (장바구니 재설계 시 같이) `.cart-sidebar` 고정 width:380px →
      `width: min(380px, 100vw)` 모바일 대응한다

**3순위 — 기능 추가 (선택)**
- [ ] coin-detail.html 관련 상품 추천 섹션을 추가한다
- [ ] coins.html 카드에 `.badge-stock`/`.badge-sameday` 뱃지를 적용한다
      (시트 데이터 연동)
- [ ] (선택) gold-price.html 더보기 탭에 이더리움/달러인덱스/미국 10년물
      금리 등 추가 지표를 검토한다 (필요하다고 판단될 시에만)

**4순위 — 디자인**
- [ ] index.html 카테고리 배너 3종 색감/이미지를 골드 톤에 맞게 교체한다
- [ ] (데스크탑 검색 기능이 구현됨에 따라) `.search-category select`를
      카테고리 필터로 실제 활용할지 결정한다
- [ ] (선택) style.css의 `.lang-section`/`.lang-options`/`.lang-opt` 죽은
      CSS 규칙을 정리한다 (이전 세션부터 이어지는 사소한 항목)

**이전 세션에서 이어지는 항목**
- [ ] contact.html "임시 주소"를 실제 주소로 교체한다 (실제 주소 정보 필요)
- [ ] 네이버 검색 노출 여부를 확인한다 → 노출 안정화되면 사이트명 변경 진행
- [ ] 구글 서치 콘솔/콘텐츠 마케팅/네이버 파워링크 (사용자 액션 필요, 보류)

## 5. 참고 사항

- 검색 기능은 현재 **데스크탑 전용**임. 모바일 메뉴에는 검색 UI가 전혀
  없다는 점을 다음 세션에서 헷갈리지 않도록 명확히 인지할 것 — 직전 직전
  문서(`2026-06-21-0102-...`)에는 모바일 검색바가 "구현 완료"로 적혀 있었으나
  바로 다음 턴에서 제거됐으므로, 그 문서를 참고할 경우 이 문서가 최신임을
  확인할 것.
- `nav.js`의 `goSearch(keyword)` 함수는 그대로 남아있고 데스크탑 검색에서
  사용 중. 모바일에 검색을 다시 추가하게 되면 이 함수를 재사용하면 됨(빈
  입력값은 자동으로 `coins.html` 전체 상품으로 이동 처리됨).
- `coins.html`의 `?q=` 필터 로직(`matchesKeyword`)은 한글 합성어 오매칭을
  막기 위해 키워드 앞뒤 글자가 한글이면 별개 단어로 보고 제외하는 방식임 —
  검색 관련 로직을 수정할 때 이 부분을 깨뜨리지 않도록 주의.
- footer.js에서 삭제한 항목들은 전부 화면에는 안 보이지만 코드 내 HTML
  주석으로 "왜 지웠는지 + 실제 정보/페이지가 생기면 다시 추가하라"는 메모를
  남겨둠. 다음 세션에서 footer.js를 열어보면 어떤 항목이 "임시 삭제"
  상태인지 주석만 보고 바로 파악 가능함.
- "오시는 길" 링크는 별도 페이지가 아니라 같은 페이지 내 위치 정보 섹션으로
  앵커 이동하는 방식(`#footer-locations`)을 택함.
- GitHub PAT 토큰: 사용자가 매 push마다 채팅에 토큰을 평문으로 입력 중.
  push 자체는 매번 성공했지만, 세션 시작 시 매번 revoke 여부를 확인해줄 것.
