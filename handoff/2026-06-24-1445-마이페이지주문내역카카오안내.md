# 마이페이지 주문 내역 카카오 안내 추가 인수인계 문서

작성일: 2026-06-24

## 1. 목표

`mypage.html`의 주문 내역 빈 상태가 "아직 주문 내역이 없습니다." 고정 문구만
표시하던 것을, 실제 운영 방식(카카오톡 문의 기반, 주문 DB 미연동)에 맞게
카카오톡 채널 안내 문구와 CTA 버튼으로 개선한다.

## 2. 현재 상태

**이번 세션 완료 (커밋 `6927195`, push 후 `git ls-remote origin HEAD`로
원격 반영 확인 완료)**

| # | 항목 | 처리 결과 |
|---|---|---|
| 1 | `mypage.html` 주문 내역 빈 상태 문구 교체 | ✅ 완료 |
| 2 | 카카오톡 문의 버튼 추가 | ✅ 완료 |

**상세 변경 내용:**
- 보조 문구를 "금화를 구매하시면..."에서 "현재 구매는 카카오톡 문의를 통해
  진행되며, 주문 내역도 카카오톡에서 확인하실 수 있습니다."로 교체
- `contact.html`에 이미 존재하던 `.kakao-btn` 패턴(노란 배경 `#FEE500` +
  카카오 아이콘 SVG + 동일 링크 `https://open.kakao.com/o/sB6Gduni`)을
  그대로 재사용해 `.mypage-empty-box` 스코프에 스타일 추가, 버튼 마크업 삽입
- 첫 문구("아직 주문 내역이 없습니다.")는 그대로 유지

## 3. 진행 단계

이번 세션 작업은 모두 완료.

1. ~~(완료) `mypage.html` 빈 상태 구조 및 사이트 내 카카오 링크 패턴 확인
   (footer.js, contact.html, coin-detail.html 3곳 grep 대조)~~
2. ~~(완료) `contact.html`의 `.kakao-btn` 스타일을 `.mypage-empty-box`
   스코프로 추가~~
3. ~~(완료) 문구 교체 및 버튼 마크업 삽입~~
4. ~~(완료) div 태그 균형, 클래스 정의-사용 일치 점검~~
5. ~~(완료) PAT으로 권한 확인 → 커밋 `6927195` → push → `git ls-remote`로
   검증 → 토큰 remote URL에서 즉시 제거~~

## 4. 다음에 진행할 일 (To-Do)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 — **2주 후** (약 2026-07-07 이후) 순서대로 점검:
  1. 색인된 페이지 수 확인 (목표: 25개 내외)
  2. "색인되지 않음" 원인 파악 → 🤖 조치
  3. 노출수·클릭수·게재순위 확인
  4. 유입 검색어로 title/description 최적화 판단 → 🤖 반영
  5. 리치 결과 탭에서 제품 스니펫 실제 노출 여부 확인
- 🔵 👤 코인 상세페이지 "관련 코인" 섹션 추가 여부 결정 (전환율 관점)
- 📢 👤 네이버 검색 노출 안정화되면 사이트명 변경 진행
- 📢 👤 콘텐츠 마케팅 / 네이버 파워링크 (보류 중)
- 🔵 👤 `coin-detail.html` JSON-LD 정적 전환 여부 결정 (우선순위 낮음)
- 🔵 👤 Organization/LocalBusiness JSON-LD 추가 여부 — 🛍️ 주소 확정 후 진행
- 🔵 👤 `apple-touch-icon` 추가 — 180×180 PNG 이미지 준비 후 진행
- 🔵 👤 `sku` 추가 — Merchant Center 연동 계획 생기면 진행

### 🛍️ 상품 데이터
- ⚪ 👤 서울 본점 실제 주소·전화번호·영업시간 확정
  → 확정 시 🤖: `footer.js` ~37번 줄 주석 해제 + 값 교체
- ⚪ 👤 강남 지점 실재 여부 확인
  → 있으면 🤖: `footer.js` ~44번 줄 주석 해제 + 실제 정보 입력
- ⚪ 👤 소셜 미디어 계정(facebook/instagram/youtube/line) 생기면
  → 🤖: `footer.js` ~51번 줄 주석 해제 + 각 계정 URL 입력
- ⚪ 👤 사업자등록번호 확정
  → 확정 시 🤖: `footer.js` ~59번 줄 주석 해제 + 값 교체
- ⚪ 👤 전화번호 확정
  → 확정 시 🤖: `contact.html` ~131번 줄 전화 카드 주석 해제 + 값 교체
- ⚪ 👤 실제 주소 확정
  → 확정 시 🤖: `contact.html` ~299번 줄 지도 섹션 주석 해제 + 주소 텍스트·embed URL 교체
  → **함께 처리**: `footer.js`의 "오시는 길" 링크를 `href="contact.html#contact-map"`으로 복원

### 🎨 UI/UX
- 🟡 👤 카테고리 배너 디자인 방향 결정 (여러 세션째 보류 중)
- 🟡 👤 뱃지 shimmer 애니메이션 제거 여부 결정 → 🤖 정적 뱃지로 교체

### ⚙️ 기능/인프라
- 해당 없음

### 📋 운영/프로세스
- 🔵 👤 `구매안내` 탭 문구(결제/배송/교환·환불 등) 실제 운영 정책 검토
  → 확정되면 🤖 `shippingDetails` / `hasMerchantReturnPolicy` JSON-LD에 반영
- 🔵 Firebase 보안 규칙 — **최종 작업 안 함으로 결정** (이전 세션 확정)

## 5. 참고 사항

- **이번 세션에서 종결된 항목**: 이전 핸드오프(`2026-06-24-1435`)의
  🎨 UI/UX "`mypage.html` 주문 내역 빈 상태 개선" 항목은 이번 세션에서
  완전히 처리됨 (코드 수정 + push + `git ls-remote` 검증까지 완료).
  다음 세션에서 다시 꺼낼 필요 없음.
- **카카오 링크 재사용 원칙**: 사이트 내 카카오톡 채널 링크는
  `https://open.kakao.com/o/sB6Gduni` 하나로 통일되어 있고(footer.js,
  contact.html, coin-detail.html, 이번에 mypage.html 추가), 새로 카카오
  관련 UI를 추가할 때는 이 링크와 `contact.html`의 `.kakao-btn` 스타일
  패턴(노란 배경 `#FEE500` + 동일 SVG 아이콘)을 그대로 재사용하면 됨.
  별도 디자인을 새로 만들 필요 없음.
- **`coins.html` 탭 카테고리 분류**: 직전 세션(`2026-06-24-1435`)에서
  최종 종결됨 — 상품이 30~40개 이상으로 늘고 은화/컬렉터블 등 다른
  카테고리가 추가될 때까지 작업 안 함. 다음 세션에서 다시 꺼낼 필요 없음.
- **GitHub PAT**: 이번 세션에서 사용 (push 1회). 만료일 미확인 — 다음
  push 시 만료 여부 먼저 확인.
- **이전 핸드오프**: `2026-06-24-1435-deadcode추가제거및탭보류종결.md`
