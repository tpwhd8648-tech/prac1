# 이용약관·개인정보처리방침 페이지 수정 인수인계 문서

작성일: 2026-06-26

## 1. 목표

다른 계정에서 신규 추가한 `terms.html` / `privacy.html`에 대해 전수조사를 진행,
발견된 6개 이슈를 모두 수정·검증 완료.

## 2. 현재 상태

### 완료된 작업 (이번 세션)

**(1) terms.html / privacy.html — nav placeholder 4종 완비 (확인 완료)**
- `top-bar` class 추가: `<div class="top-bar" id="nav-topbar">`
- `nav-main` div 추가: `<nav id="nav-main"></nav>`
- `nav-mobile` div 추가: `<div id="nav-mobile"></div>`
- 기존 6개 페이지(index/coins/contact/gold-price/mypage/coin-detail)와 동일 구조

**(2) OG / Twitter 메타태그 추가 (확인 완료)**
- terms.html, privacy.html 양쪽에 `og:type` / `og:url` / `og:title` / `og:description` /
  `og:image` / `og:site_name` / `og:locale` / `twitter:card` / `twitter:title` /
  `twitter:description` / `twitter:image` 전체 추가
- 이미지는 기존 페이지와 동일하게 `images/products/2026/maple.png` 사용

**(3) privacy.html 6번 항목 문구 수정 (확인 완료)**
- 수정 전: "마이페이지를 통해 탈퇴를 요청할 수 있습니다" → 실제로는 탈퇴 기능 없음
- 수정 후: "마이페이지를 통해 본인의 로그인 정보를 확인할 수 있습니다. 탈퇴·열람·정정·삭제 요청은 카카오톡 채널로"
- 마이페이지에서 실제 가능한 기능(로그인 정보 확인)은 유지, 없는 기능(탈퇴 UI) 언급 제거

**(4) nav.js 회원가입 모달 — 약관 동의 체크박스 추가 (확인 완료)**
- signup 탭 전환 시 체크박스 노출, signin 탭 전환 시 숨김
- 미체크 상태로 가입 시도 시 "이용약관 및 개인정보처리방침에 동의해 주세요." 에러
- 약관/개인정보 링크 클릭 시 `stopPropagation()`으로 체크박스 자동 토글 방지
- `rel="noopener"` 포함, `target="_blank"`로 새 탭에서 열림

**(5) style.css `.auth-agree` 스타일 추가 (확인 완료)**
- `accent-color: var(--gold)`, 링크 금색 underline, `align-items: flex-start` 등

### 세션 중 발견·정정된 사항
- 최초 전수조사(이번 계정)에서 `nav-mobile` / `nav-main` 누락을 미탐지 — placeholder를
  3종으로 잘못 체크한 것이 원인. 다른 계정 검토에서 발견, 이번 세션에서 보완 완료.
- 이번 계정 수정본에는 `style.css` 체크박스 스타일이 없었음 — 다른 계정 버전에서 추가됨.
- `개인정보를 안 쓴다`는 전제 오류 정정: 이메일은 개인정보보호법상 개인정보이며,
  `auth.js`의 `createUserWithEmailAndPassword`가 실제 수집 중이므로 약관 동의 체크박스는
  법적 필수 절차임을 확인.

### 현재 레포 상태
- 최신 커밋: `7e42ddf` (nav-main 누락 추가)
- `terms.html`, `privacy.html`, `nav.js`, `style.css` 4개 파일 수정 완료

## 3. 진행 단계

해당 없음 (이번 세션 작업 전부 완료).

## 4. 다음에 진행할 일 (To-Do)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 (2026-07-07 이후 — 색인 수, 미색인 사유, 리치 결과 여부)
- 🔵 👤 네이버 검색 노출 안정화 후 사이트명 변경 진행
- 🔵 👤 FAQ 페이지 분리 여부 결정 (분리 시 FAQPage 스키마 적용 가능) → 🤖 코드 반영

### 🛍️ 상품 데이터
- 🔴 👤 사업자등록번호·통신판매업신고번호·대표자명·실제 주소·연락처 확정 단계 확인
  (등록 전이면 가짜 placeholder를 진짜 숨김 처리로 전환 검토) → 🤖 footer.js/contact.html 반영
- ⚪ 👤 사업자등록번호·소셜 미디어 계정 확정 → 🤖 footer.js 반영
- ⚪ 👤 서울 본점 실제 주소·전화번호·영업시간 확정 → 🤖 footer.js 반영

### 🎨 UI/UX
- 🟡 👤 카테고리 배너·히어로 디자인 방향 결정 (A/B/C 중 또는 별도, placeholder 이미지 교체 포함) → 🤖 코드 반영
- 🟡 👤 가격 정렬(낮은순/높은순) 기능 추가 여부 결정 → 🤖 coins.html 반영
- 🟡 👤 위시리스트(찜하기) 기능 도입 여부 결정 → 🤖 진입점 버튼 + mypage 섹션 복원
- 🔵 👤 가격 알림 기능 도입 여부 결정 (인프라 비용 큼, 신중 검토)
- 🔵 👤 검색 결과 0건 시 안내 배너 추가 여부 결정 → 🤖 coins.html 반영

### ⚙️ 기능/인프라
- 🟡 👤 mypage.html 회원 탈퇴 기능 추가 여부 결정
  (현재 privacy.html에서 카카오톡 채널로 안내 중이나, 직접 탈퇴 UI가 있으면 더 완결됨) → 🤖 반영
- 🔵 👤 단위 환산 계산기(1돈=3.75g 등) 추가 여부 결정 → 🤖 gold-price.html 또는 별도 위젯 반영

### 📋 운영/프로세스
- 🔵 👤 구매안내 탭 문구(결제/배송/교환·환불) 실제 운영 정책 검토 후 → 🤖 반영

## 5. 참고 사항

- **placeholder 체크 기준 업데이트**: 향후 신규 페이지 추가 시 확인할 placeholder는
  `nav-topbar(top-bar class 포함)` / `nav-header` / `nav-main` / `nav-mobile` / `site-footer`
  **5종**. 이번 세션에서 3종만 체크한 것이 `nav-main` / `nav-mobile` 미탐지 원인이었음.
- **약관 동의 체크박스 ID**: `#auth-agree-wrap` (label), `#auth-agree-checkbox` (input).
  nav.js에서 querySelector로 참조 중이므로 id 변경 시 JS도 함께 수정 필요.
- **terms.html / privacy.html은 `noindex`**: sitemap 미등록이 올바른 처리. 추후 사업자
  정보 확정 후 정식 약관으로 교체할 때 `noindex` 유지 여부 재검토.
- **mypage 탈퇴 기능**: 현재 없음. privacy.html은 카카오톡 채널 안내로 우회 처리 중.
  탈퇴 UI 추가 시 Firebase `deleteUser()` 연동 필요.
- **이전 핸드오프**: `2026-06-26-1445-사이트기능점검및우선순위제안.md`
