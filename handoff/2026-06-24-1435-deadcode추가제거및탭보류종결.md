# dead code 추가 제거 및 coins.html 탭 보류 종결 인수인계 문서

작성일: 2026-06-24

## 1. 목표

이전 세션에서 누락됐던 인수인계 문서 push 완료 후,
`style.css`에 남아 있던 dead CSS를 추가로 전수 점검해 제거하고,
`coins.html` 탭 분류 작업의 보류/종결 여부를 최종 결정한다.

## 2. 현재 상태

**이번 세션 완료 (커밋 `b565007`, `b6eefb9`, push 확인)**

| # | 항목 | 처리 결과 |
|---|---|---|
| 1 | 인수인계 문서 누락 push | ✅ `c36fb1e`으로 push 완료 |
| 2 | `style.css` 빈 `.btn-cart-wrap { }` | ✅ 제거 (`b565007`) |
| 3 | `style.css` `.anti-fraud` | ✅ 제거 (`b6eefb9`) |
| 4 | `style.css` `.check-all-btn` (데스크탑·hover·모바일 3줄) | ✅ 제거 (`b6eefb9`) |
| 5 | `style.css` `.price-val.palladium` | ✅ 제거 (`b6eefb9`) |
| 6 | `coins.html` 탭 카테고리 분류 | ✅ **종결** — 작업 안 함으로 확정 |

**dead code 검증 방법**: Python으로 `.html` · `.js` · `.css` · `.md` 전 파일 전수 검색,
`style.css` 외 어느 파일에도 참조 없음 확인 후 제거.

**6번 종결 근거**: 현재 상품 20개 전부 금화 1oz 단일 카테고리.
나라별/순도별 어느 기준으로 분류해도 탭당 상품 수가 너무 적어 UX 저하.
은화·컬렉터블 등 다른 카테고리 상품이 추가돼 전체 상품 수가 30~40개 이상이 될 때 재검토.
`products.js`의 `getCategoryFilter()`는 이미 `silver`, `collectible` 카테고리를 지원하므로
그때 탭 버튼 HTML만 추가하면 됨.

## 3. 진행 단계

이번 세션 작업은 모두 완료.

1. ~~(완료) 인수인계 문서 누락 확인 및 push~~
2. ~~(완료) style.css 전 파일 dead code 전수 점검~~
3. ~~(완료) 빈 `.btn-cart-wrap { }` 제거 및 push~~
4. ~~(완료) `.anti-fraud` · `.check-all-btn` · `.price-val.palladium` 제거 및 push~~
5. ~~(완료) coins.html 탭 분류 작업 종결 결정~~

## 4. 다음에 진행할 일 (To-Do)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 — **2주 후** (약 2026-07-07 이후) 순서대로 점검:
  1. 색인된 페이지 수 확인 (목표: 25개 내외)
  2. "색인되지 않음" 원인 파악 → 🤖 조치
  3. 노출수·클릭수·게재순위 확인
  4. 유입 검색어로 title/description 최적화 판단 → 🤖 반영
  5. 리치 결과 탭에서 제품 스니펫 실제 노출 여부 확인
- 🔵 👤 코인 상세페이지 "관련 코인" 섹션 추가 여부 결정 (전환율 관점)
- 📢 👤 네이버 검색 노출 안정화되면 사이트명 변경 진행
- 📢 👤 콘텐츠 마케팅 / 네이버 파워링크 (보류 중)
- 🔵 👤 `coin-detail.html` JSON-LD 정적 전환 여부 결정 (우선순위 낮음)
- 🔵 👤 Organization/LocalBusiness JSON-LD 추가 여부 — 🛍️ 주소 확정 후 진행
- 🔵 👤 `apple-touch-icon` 추가 — 180×180 PNG 이미지 준비 후 진행
- 🔵 👤 `sku` 추가 — Merchant Center 연동 계획 생기면 진행

### 🛍️ 상품 데이터
- ⚪ 👤 서울 본점 실제 주소·전화번호·영업시간 확정
  → 확정 시 🤖: `footer.js` ~37번 줄 주석 해제 + 값 교체
- ⚪ 👤 강남 지점 실재 여부 확인
  → 있으면 🤖: `footer.js` ~44번 줄 주석 해제 + 실제 정보 입력
- ⚪ 👤 소셜 미디어 계정(facebook/instagram/youtube/line) 생기면
  → 🤖: `footer.js` ~51번 줄 주석 해제 + 각 계정 URL 입력
- ⚪ 👤 사업자등록번호 확정
  → 확정 시 🤖: `footer.js` ~59번 줄 주석 해제 + 값 교체
- ⚪ 👤 전화번호 확정
  → 확정 시 🤖: `contact.html` ~131번 줄 전화 카드 주석 해제 + 값 교체
- ⚪ 👤 실제 주소 확정
  → 확정 시 🤖: `contact.html` ~299번 줄 지도 섹션 주석 해제 + 주소 텍스트·embed URL 교체
  → **함께 처리**: `footer.js`의 "오시는 길" 링크를 `href="contact.html#contact-map"`으로 복원

### 🎨 UI/UX
- 🟡 🤖 `mypage.html` 주문 내역 빈 상태 개선
  → 카카오 채널 구매 문의 안내 또는 구매 가이드 문구로 교체
- 🟡 👤 카테고리 배너 디자인 방향 결정 (여러 세션째 보류 중)
- 🟡 👤 뱃지 shimmer 애니메이션 제거 여부 결정 → 🤖 정적 뱃지로 교체

### ⚙️ 기능/인프라
- 해당 없음

### 📋 운영/프로세스
- 🔵 👤 `구매안내` 탭 문구(결제/배송/교환·환불 등) 실제 운영 정책 검토
  → 확정되면 🤖 `shippingDetails` / `hasMerchantReturnPolicy` JSON-LD에 반영
- 🔵 Firebase 보안 규칙 — **최종 작업 안 함으로 결정** (이전 세션 확정)

## 5. 참고 사항

- **이번 세션에서 최종 종결된 항목**:
  - `coins.html` 탭 카테고리 분류: 상품이 충분히 많아질 때(30~40개+, 다른 금속 카테고리 추가 시)까지 작업 안 함. 다음 세션에서 다시 꺼낼 필요 없음.
- **탭 추가 시 필요한 작업 (나중에 참고)**:
  - `coins.html` `<div class="product-tabs">` 안에 `<button class="ptab" data-filter="silver">은화</button>` 식으로 버튼 추가만 하면 됨
  - `products.js` `getCategoryFilter()`는 이미 `silver`, `collectible` 지원 중 — JS/CSS 수정 불필요
- **style.css dead code 전수 점검 완료**: `.facebook` · `.instagram` · `.youtube` · `.line` 은 footer.js 주석 해제 시 사용될 의도적 보류 CSS — dead code 아님, 건드리지 말 것.
- **GitHub PAT**: 이번 세션에서 사용 (push 3회). 만료일 미확인 — 다음 push 시 만료 여부 먼저 확인.
- **이전 핸드오프**: `2026-06-24-1424-deadcode제거및카드CSS통합.md`
