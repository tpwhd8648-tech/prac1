# To-Do 정리 (검색 0건 배너 보류, 항목 2건 종결) 인수인계 문서

작성일: 2026-06-29 (KST)

## 1. 목표

직전 핸드오프(`2026-06-29-1433`)의 To-Do 목록을 사용자와 함께 재검토하여,
실제로 더 진행할 필요가 없는 항목 3건을 명시적으로 종결 처리함. 코드 변경은
없고 To-Do 목록 정리만 진행한 세션.

## 2. 현재 상태

### (1) "검색 결과 0건 시 안내 배너" — 보류 결정 (확인 완료)
- `pages/coins.html`의 `renderProducts()` 코드를 직접 확인한 결과, 검색
  키워드와 일치하는 상품이 0건이면 `matched = false`가 되어 **필터를 적용하지
  않고 전체 상품 목록을 그대로 보여주는 구조**임 (706~711번 줄). 배너도
  `matched`가 false면 띄우지 않음 (717번 줄 조건).
- 즉 "빈 결과 화면"이라는 상황 자체가 현재 구조에서는 발생하지 않음 — 검색이
  실패해도 사용자는 항상 상품이 채워진 화면을 보게 됨.
- 사용자 판단: 별도 0건 안내 배너를 추가하면 오히려 "검색이 실패했다"는
  인상을 줘서 지금의 자연스러운 전체목록 fallback보다 UX가 나빠질 수 있다고
  보고, 추가하지 않기로 결정.
- **재검토가 필요해지는 경우**: 향후 fallback 정책 자체를 바꿔서 "0건이면
  진짜 빈 화면을 보여준다"로 구조를 바꾸게 되면, 그때는 안내 배너가 다시
  필요해질 수 있음.

### (2) "다른 계정 핸드오프 작성 누락 재발 방지" — 종결 (운영 메모, 코드 무관)
- `2026-06-27-1306` 핸드오프에서 처음 등록된 항목. 당시 다른 계정 세션에서
  `pages/` 폴더 이동이라는 구조 변경을 진행했는데 핸드오프 문서를 남기지
  않아, 후속 세션이 `git log`로 직접 추적해야 했던 사례에 대한 재발 방지
  메모였음.
- 코드로 해결할 수 있는 항목이 아니라 "다음에 다른 계정에서 작업 마칠 때
  핸드오프 작성을 잊지 말 것"이라는 운영 리마인더 성격. 사용자가 내용을
  확인한 뒤 한 번 짚었으니 충분하다고 보고 To-Do에서 제외하기로 결정.

### (3) `products.js`의 `linkTemplate` 기본 폴백값 — 이미 해결됨 확인, 종결
- `2026-06-27-1306`/`2026-06-29-1433` 두 핸드오프에 "기본 폴백값이 여전히
  `'coins.html'`(이동 전 경로)로 남아있다"고 적혀 있었으나, 이번 세션에서
  `assets/js/products.js` 61번째 줄을 직접 확인한 결과 이미
  `'/pages/coins.html'`로 수정되어 있었음 (확인 완료).
- git log로 원인 추적: 커밋 `af8c452`(`fix: products.js linkTemplate 폴백값
  경로 수정`)에서 이미 수정·반영됨. 이 커밋이 `2026-06-27-1306` 핸드오프
  작성 이후, `2026-06-29-1433` 핸드오프 작성 이전 사이에 있었던 것으로
  보이는데, 두 핸드오프 모두 이 변경을 반영하지 못하고 옛 내용을 그대로
  복사해 옮긴 것으로 추정됨 (직접 검증: `git show af8c452 --
  assets/js/products.js`로 diff 확인).
- 항목 자체가 더 이상 사실과 맞지 않으므로 To-Do에서 제외.

### 현재 레포 상태
- 최신 커밋: `0a538f8` (변경 없음 — 이번 세션은 코드 수정 없이 To-Do 정리만
  진행)

## 3. 진행 단계

해당 없음 (이번 세션은 To-Do 재검토 및 문서화만 진행, 추가 코드 작업 없음).

## 4. 다음에 진행할 일 (To-Do)

(직전 2개 핸드오프와 교차 확인 — 이번 세션에서 3개 항목 종결, 나머지
미해결 항목 동일 유지)

### 🔍 SEO
- 📢 👤 구글 서치 콘솔 데이터 확인 (2026-07-07 이후 — 색인 수, 미색인 사유, 리치 결과 여부)
- 🔵 👤 네이버 검색 노출 안정화 후 사이트명 변경 진행
- 🔵 👤 FAQ 페이지 분리 여부 결정 (분리 시 FAQPage 스키마 적용 가능) → 🤖 코드 반영
- 🟡 👤 `coins.html`/`contact.html`/`gold-price.html`/`mypage.html` URL이
  `/`에서 `/pages/`로 바뀐 데 따른 검색 노출 변동 여부 모니터링 (301 리다이렉트
  불가능한 정적 GitHub Pages 특성상 불가피, 결과만 추적)

### 🛍️ 상품 데이터
- 🔴 👤 사업자등록번호·통신판매업신고번호·대표자명·실제 주소·연락처 확정 단계 확인
  (등록 전이면 가짜 placeholder를 진짜 숨김 처리로 전환 검토) → 🤖 footer.js/contact.html 반영
- ⚪ 👤 사업자등록번호·소셜 미디어 계정 확정 → 🤖 assets/js/footer.js 반영
- ⚪ 👤 서울 본점 실제 주소·전화번호·영업시간 확정 → 🤖 assets/js/footer.js 반영
- 🟡 👤 PRE SALE 코인 10개(크루거랜드/판다/성조지/퀸즈라이언/라이언이글/
  스완/체코라이언/아웃백/케이브라이언/로얄드래곤) 구글 시트 D열(premium)
  확인 → 현재 `1`(마진 0% placeholder)로 입력되어 있어, 가격 정렬 시
  실제로 싼 게 아니라 데이터 미입력 상태로 보일 수 있음. 정상 premium
  값 입력 시 다음 SSG 빌드에서 자동 반영(코드 수정 불필요)

### 🎨 UI/UX
- 🟡 👤 카테고리 배너·히어로 디자인 방향 결정 (placeholder 이미지 교체 포함) → 🤖 코드 반영
- 🟡 👤 위시리스트(찜하기) 기능 도입 여부 결정 → 🤖 진입점 버튼 + mypage 섹션 복원
- 🔵 👤 가격 알림 기능 도입 여부 결정 (인프라 비용 큼, 신중 검토)
- ⚪ 👤 coin-detail.html 정리(삭제) 여부 재검토 필요 시 결정 → 🤖 fallback
  로직(coins.html/generate-coin-pages.js 2곳) 함께 정리. 단, 새 코인 추가 시
  IMAGE_MAP 매칭 실패에 대한 안전망 역할을 하므로 신중 검토 권장.

### ⚙️ 기능/인프라
- 🟡 👤 mypage.html 회원 탈퇴 기능 추가 여부 결정 → 🤖 반영
- 🔵 👤 단위 환산 계산기(1돈=3.75g 등) 추가 여부 결정 → 🤖 gold-price.html 또는 별도 위젯

### 📋 운영/프로세스
- 🔵 👤 구매안내 탭 문구(결제/배송/교환·환불) 실제 운영 정책 검토 후 → 🤖 반영

**이번 세션에서 종결된 항목 (다음 세션에서 되살리지 않음)**:
- ~~검색 결과 0건 시 안내 배너 추가~~ → 보류 결정 (현재 fallback 구조상
  불필요, 위 2-(1) 참고)
- ~~다른 계정 핸드오프 작성 누락 재발 방지~~ → 운영 리마인더 1회 확인 완료로
  종결
- ~~`products.js`의 `linkTemplate` 기본 폴백값 정리~~ → 커밋 `af8c452`에서
  이미 해결된 것으로 확인, 항목 자체가 사실과 불일치하여 종결

## 5. 참고 사항

- **핸드오프 문서가 옛 정보를 그대로 복사해 옮기는 문제**: 이번에 발견한
  `linkTemplate` 사례처럼, 어떤 항목이 실제로는 이미 해결됐는데도 핸드오프
  문서끼리 교차 확인할 때 "명시적 종결 표시"가 없으면 계속 복사되어 옮겨질
  수 있음. To-Do 항목을 다음 문서로 옮길 때는 가능하면 관련 코드를 다시 한번
  직접 확인(grep/view)하고 옮기는 것이 안전함 — 특히 "현재 ~한 상태로
  남아있음"처럼 현재 상태를 단정하는 문구가 있는 항목은 재검증 우선순위가
  높음.
- **이전 핸드오프**: `2026-06-29-1433-coin-detail-noindex-가격정렬추가.md`
